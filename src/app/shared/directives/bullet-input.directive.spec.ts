import { BulletInputDirective } from './bullet-input.directive';

describe('BulletInputDirective', () => {
  it('should create an instance', () => {
    const directive = new BulletInputDirective();
    expect(directive).toBeTruthy();
  });
});
